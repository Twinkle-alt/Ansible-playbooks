---
 - hosts: all
   tasks:
     - name: Display Username
       debug:
         msg: "Entered Username is {{ username }}"

     - name: Use Password Securely
       debug:
         msg: "Password received successfully"
